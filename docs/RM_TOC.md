# `RM` table of contents — every roadmap item and where it actually lives

**The single complete list.** Nothing else was: [ROADMAP.md](ROADMAP.md)'s detail table covered
RM4–RM7 / RM10–RM17 / RM20–RM27, [USE_CASES.md](USE_CASES.md)'s covered RM1–RM14 / RM18–RM22, neither
was sorted, RM28–RM35 lived only as prose paragraphs, and the major-version items are not numbered at
all. So `RM33` was genuinely unfindable. This file exists to make that impossible. ROADMAP.md's active
list ran empty after the 0.6 batch, filled again with RM74–RM79, and emptied again when those shipped —
which does not retire the argument in either direction. An item that moves leaves a pointer at where it
used to live, and that is the same defect one document over.

**Add an entry here whenever you add an item, and keep it sorted.** If you find yourself wanting a
second index somewhere else, don't — two partial lists of the same 35 items is what caused this, and
there are 87 now.

Format of an entry: the **number** links to the authoritative entry, the one to edit. *also in* lists
every other document that mentions it, so a rename or a status change can be propagated without
grepping.

---

## ✅ Shipped

**0.4**

- **[RM1](USE_CASES.md#roadmap-items-surfaced)** — compiler materializes every 0.4 table → parquet, lossless round-trip. · *also in* ROADMAP, CHANGELOG
- **[RM2](USE_CASES.md#roadmap-items-surfaced)** — composed modules: `variants.csv` optional, a module carries only the kinds it uses. · *also in* ROADMAP, CHANGELOG, SCHEMAS
- **[RM3](USE_CASES.md#roadmap-items-surfaced)** ⚠ — PharmGKB row shape. **Superseded by RM20**, and the cautionary row: marked shipped against a hand-authored sample, then the real corpus rejected ~97% of itself. · *also in* ROADMAP, PROPOSAL_0_5
- **[RM8](USE_CASES.md#roadmap-items-surfaced)** — generated authoring reference (`authoring_reference()` / `json_schemas()`), reachable since 0.5 as `just-dna-compiler reference`. · *also in* ROADMAP, SCHEMAS, COMPILER, CHANGELOG
- **[RM9](USE_CASES.md#roadmap-items-surfaced)** — recommended colour / icon palette (`RECOMMENDED_COLORS` / `RECOMMENDED_ICONS`). · *also in* ROADMAP, COMPILER, CHANGELOG
- **[RM11](ROADMAP_HISTORY.md#rm11--doi-provenance-column-on-studyrow)** — `doi` provenance column on `StudyRow`. · *also in* USE_CASES, SCHEMAS, COMPILER, CHANGELOG
- **[RM12](ROADMAP_HISTORY.md#rm12--provenance-locator-provenance_quote--provenance_regex)** — provenance locator: `provenance_quote` / `provenance_regex`. · *also in* USE_CASES, SCHEMAS, COMPILER, CHANGELOG
- **[RM14](ROADMAP_HISTORY.md#rm14--structured-per-version-authorship)** — structured per-version `authorship` (identity / role / kind). · *also in* USE_CASES, COMPILER, CHANGELOG

**0.5**

- **[RM6](ROADMAP_HISTORY.md#rm6--callability-as-first-class-state)** — callability as first-class state: `requires_callable` + `callable_from`. · *also in* USE_CASES, SCHEMAS, COMPILER, CHANGELOG, PROPOSAL_0_5
- **[RM13](ROADMAP_HISTORY.md#rm13--the-network-first-resolution-tier)** — network-first resolution tier, **realized as `just-dna-enricher`**. · *also in* USE_CASES, CHANGELOG, PROPOSAL_0_5
- **[RM17](ROADMAP_HISTORY.md#rm17--semver-on-moduleversion-coercing)** — SemVer enforcement on `module.version`, coercing. · *also in* COMPILER, CHANGELOG, PROPOSAL_0_5
- **[RM18](USE_CASES.md#roadmap-items-surfaced)** — frequency + gene-constraint sidecars (`frequencies.csv`, `gene_metrics.csv`). · *also in* ROADMAP
- **[RM19](USE_CASES.md#roadmap-items-surfaced)** — GA4GH VRS allele identity: `vrs_id`, `caid`, VA-derived `variant_key`. Satisfies RM15's build-naming condition; multi-build minting stays RM15. · *also in* ROADMAP
- **[RM20](ROADMAP_HISTORY.md#rm20--pharmgkb-annotations-are-per-genotype-and-per-category)** — PharmGKB annotations are per-genotype **and** per-category. Corrects RM3. · *also in* USE_CASES, PROPOSAL_0_5
- **[RM21](ROADMAP_HISTORY.md#rm21--data-source-licensing-as-data)** — data-source licensing as data: `sources.csv` + the compile gate. · *also in* USE_CASES, COMPILER, ENRICHER, CHANGELOG, PROPOSAL_0_5
- **[RM22](ROADMAP_HISTORY.md#rm22--pgx-tables-join-resolution)** — PGx tables join resolution (`enrich()` reads the PGx CSVs too). · *also in* USE_CASES, PROPOSAL_0_5

*Found by dogfooding on 2026-08-03 — the batch of five that produced RM31–RM35. Four were fixed in that same window and are listed here; **RM32** was held back as a question about identity and shipped in 0.5, below. Each entry keeps the probe, the repairs that stayed rejected, and (for three of the five) the part of the original argument that turned out to be wrong on probing.*

- **[RM31](ROADMAP_HISTORY.md#rm31--one-indel-spelled-two-ways-defeats-allele-aware-resolution)** — ✅ **shipped in 0.5**: ClinVar's `X:634689 CAG>C` and Ensembl's `X:634690 AGAG>AG` are the same 2 bp deletion, and string comparison called it `not_found`. Closed by a frame-free parsimony reduction with a tri-state verdict; one residual (the authored genotype keeps its source's frame) is stated there. · *also in* CLAUDE.md, SCHEMAS, COMPILER, CHANGELOG
- **[RM33](ROADMAP_HISTORY.md#rm33--source-names-two-different-things-in-two-tables)** — ✅ **shipped in 0.5**: `source` named a *link* in `resolution.csv` and a *licensed source* in `sources.csv`, compared by string equality. Closed by `ResolutionRow.authority` + the link→authority map in the enricher; the two repairs the entry rejected stayed rejected. · *also in* CLAUDE.md, SCHEMAS, ENRICHER, CHANGELOG
- **[RM34](ROADMAP_HISTORY.md#rm34--the-cpic-provider-has-no-filter)** — ✅ **shipped in 0.5**: CYP2D6 drafted 16,290 rows, 73% `Indeterminate`, with no way to take a subset. Closed by `draft --allele`, applied to all three tables; six alleles make CYP2D6 21 diplotypes. · *also in* ENRICHER, CHANGELOG
- **[RM35](ROADMAP_HISTORY.md#rm35--a-continuous-binning-table-cannot-be-tiled-without-a-finding)** — ✅ **shipped in 0.5**: inclusive bounds + overlap-is-error + hole-is-warning were jointly unsatisfiable on a continuous measure. Resolved as *a shared endpoint is a boundary and the higher bin owns it*; half-open bounds lost on authorship. · *also in* CLAUDE.md, SCHEMAS, create-module skill, CHANGELOG
- **[RM37](ROADMAP_HISTORY.md#rm37--content_signature-counted-where-a-value-was-written)** — ✅ **shipped in 0.5**: `compile → reverse → compile` moved `content_signature` for a module authoring `curator`/`method` per row, because reverse re-emits the value under `defaults:` and the hash read the CSVs before defaults applied. Closed by resolving `defaults:` into each row *before* hashing, reusing RM36's omit-the-model-default normalization so only a module stating something non-built-in moves (one of eleven reference examples). Also closed an unfiled defect: two modules differing only in `defaults.curator` hashed **equal**. The other two candidate repairs stayed rejected, with reasons. · *also in* CLAUDE.md, create-module skill, CHANGELOG
- **[RM36](ROADMAP_HISTORY.md#rm36--a-model-property-cannot-know-its-modules-build)** — ✅ **shipped in 0.5**: a model *property* that mints an identity had no module in scope, so `HeteroplasmyRow.variant_key` gave a GRCh37 module's locus a GRCh38 VA while `variants.csv` gave it a coordinate key. Closed by **injection** — the loader *tells* each row the build via a `PrivateAttr`, so the declaration stays in `module_spec.yaml` alone and reaches no CSV, parquet or digest. Per-row and per-CSV ("service row") declaration were both rejected, with reasons. Carried the correction that `content_signature` was **not** build-independent: it now hashes a non-default `genome_build`, leaving every GRCh38 module's signature untouched. · *also in* CLAUDE.md, SCHEMAS, CHANGELOG

*The post-cut round of the same 0.5.0, which was still unpublished at the time. These carried a `0.5.1`
label for a while; no 0.5.x had been published, so they shipped as 0.5.0 like everything above — see the
note at the top of [CHANGELOG.md](CHANGELOG.md). **0.5.0 released 2026-08-07.***

- **[RM26](ROADMAP_HISTORY.md#rm26--all-three-drafting-providers)** — all three drafting providers: CPIC, ClinPGx, ClinVar. Partially dissolves RM4. · *also in* ENRICHER, CHANGELOG
- **[RM29](ROADMAP_HISTORY.md#rm29--cofactor-columns)** — cofactor columns: `quality_from` / `min_quality` on `VariantRow`, `clinical_context` on `DiplotypeRow`. Dissolved the `draft --population` refusal. · *also in* CHANGELOG
- **[RM30](ROADMAP_HISTORY.md#rm30--one-rule-for-a-haplotype-name-across-all-three-pgx-tables)** — one haplotype-name rule across all three PGx tables. · *also in* REFERENCE_EXAMPLES, CHANGELOG
- **[RM32](ROADMAP_HISTORY.md#rm32--a-pseudoautosomal-locus-is-one-place-on-two-contigs)** — ✅ **shipped in 0.5**, the fifth of the 2026-08-03 dogfooding batch and the one held back as a question: a pseudoautosomal locus is one place on two contigs, modelled as two variants (10 SHOX findings → 20 rows). The probe the entry named **refuted** its own preferred direction — ClinGen mints two CA ids per PAR base, so there is no place identity to adopt — while the objection that had parked the enricher policy also failed: ClinVar and gnomAD place PAR annotation on X exclusively, so selecting X records the *sources'* convention, not the consumer's analysis set. Closed by `vrs.par_partner` + X-spelling selection (`--keep-par-twin` keeps both), per locus because XG and SPRY3 straddle a boundary. Carried a false-absence fix in `frequencies.csv` (`not_covered`) with it. · *also in* CLAUDE.md, SCHEMAS, ENRICHER, COMPILER, create-module skill, REFERENCE_EXAMPLES, CHANGELOG

**0.5.1 — the network tier alone (enricher + compiler; format untouched at 0.5.0)**

*Not a schema release. Every item is enricher/compiler API shape, out of `artifact.digest`, and touches
no parquet — which is what makes a patch legal inside the closed 0.5 digest window (P3/P8). RM38 came
from design; RM39–RM42 are a `just-dna-registry` **consumer field report**, and the through-line is one
this codebase already makes elsewhere: a number this workspace computed and then discarded gets
recomputed by every consumer, and a recomputation is a place to drift.*

- **[RM38](ROADMAP_HISTORY.md#rm38--a-cache-for-every-gated-source-the-hosted-enricher)** — ✅ **shipped in 0.5.1**: the three licence-gated PGx sources were the only ones with no cache, so a **hosted** enricher either fetched them live per request on the operator's own credentials or skipped the check. Closed by `cpic_build`/`pharmvar_build`, `locations.resolve_{cpic,pharmvar,clinpgx}_reference`, `download.ensure_{cpic,clinpgx}_snapshot`, duckdb snapshot clients duck-typed against the live ones, and a real `--offline` on `pgx`/`draft`. **PharmVar is build-only** — its key is personal and non-transferable, so no publish and no `ensure_*`. Two prerequisite defects fixed on the way (`LICENSE.txt` never published; the layout constants imported from a `[dev]` builder) and two integration defects found by probing (PharmVar's GRCh37-first coordinates; CPIC's `gene.chr`). · *also in* CLAUDE.md, ENRICHER, CHANGELOG
- **[RM39](ROADMAP_HISTORY.md#rm39--one-pass-in-the-family-ignored-offline)** — ✅ **shipped in 0.5.1**: `enrich_dosage_sensitivity` was the only pass with no `offline` parameter, so one member of a family run under one flag egressed on a path documented as making none. Closed additively: the flag, `ClinGenResult.skipped_offline`, `--offline` on `dosage`. An injected `curation_text` still wins — that is not egress. · *also in* ENRICHER, CHANGELOG
- **[RM40](ROADMAP_HISTORY.md#rm40--vrs-coverage-was-computed-and-thrown-away)** — ✅ **shipped in 0.5.1**: `enrich()` computed the `MintResult` the compiler later stamps into the manifest and dropped it, so a pre-compile consumer re-implemented per-ALT-slot counting and could disagree with the manifest a publish would produce. Closed by `EnrichmentResult.vrs`; `None` when the pass did not run, never a coverage of zero. · *also in* ENRICHER, CHANGELOG
- **[RM41](ROADMAP_HISTORY.md#rm41--the-only-correct-csv-loader-was-private)** — ✅ **shipped in 0.5.1**: `compiler._load_csv_rows` was private and the only correct authored-CSV loader, so consumers chose between a private symbol and a re-implementation with two known traps. Closed both ways: `compiler.load_csv_rows` is public (old name kept as an alias), `compiler.load_spec_variants` does the build injection and re-stamp, and `verify_acmg_sf`/`check_identifiers` accept `spec_dir=` beside `variants=`. · *also in* COMPILER, ENRICHER, CHANGELOG
- **[RM42](ROADMAP_HISTORY.md#rm42--the-retry-ceiling-was-an-import-time-constant)** — ✅ **shipped in 0.5.1**: nine `stop_after_attempt(3..4)` decorator arguments with no setting, so a server inside an unattended publish could not ask for more persistence than an author at a terminal wants; a consumer was walking the package to reassign `policy.stop`. Closed by `net.attempt_floor` + `$JUST_DNA_HTTP_RETRY_ATTEMPTS` — a **floor** that preserves the deliberate per-client tuning, never a flat set. · *also in* ENRICHER, CHANGELOG


## ✅ Also shipped in 0.6.0 — open items, not design-round decisions

*Why these three sit apart from the section below: they were already open `RMn` items when the 0.6
cycle started and landed together on 2026-08-12, a day before the design round, so their outcomes live
with the other shipped items under
[ROADMAP_HISTORY § Shipped items](ROADMAP_HISTORY.md#shipped-items) rather than in that file's
[§ 0.6.0](ROADMAP_HISTORY.md#060--the-design-round-built) section, and none of them was argued in
[PROPOSAL_0_6.md](PROPOSAL_0_6.md). Same release, different provenance.*

- **[RM44](ROADMAP_HISTORY.md#rm44--fully_resolved-answers-a-question-nobody-asked-it-and-prose-is-the-only-record-of-the-real-one)** — ✅ **shipped in 0.6.0**: `fully_resolved` is `all()` over `variants.csv`, hence **vacuously true** for a table-only module, and a catalog following the documented trust rule served `trusted: true` for modules that join to no VCF. Fixed downstream by substring-matching the 0.5.3 warning's prose, which makes a sentence load-bearing (`compiler.UNJOINABLE_PHRASE` pins it meanwhile). The fix is one additive integer — `resolution_subjects` — beside the flag, the same parts-not-conveniences pattern as `vrs_alleles`. Do **not** make the flag `None`-able, and it does **not** retire `UNJOINABLE_PHRASE` (that count belongs with RM43). Counted after rsID expansion, since that is the list the flag iterates. One thing the item missed and the history entry records: the number was already available as `Stats.weights_rows` — publishing it beside the flag is still right, because that equality is a property of the materializer rather than a contract and `Stats` is display facets, but *check for an existing carrier before adding a computed field*. · *also in* CLAUDE.md, COMPILER, CHANGELOG, CONSUMER_SUGGESTIONS_HISTORY § S13

- **[RM49](ROADMAP_HISTORY.md#rm49--a-spec-directory-is-flat-so-a-legible-derived-layout-is-one-the-compiler-refuses)** — ✅ **shipped in 0.6.0**, together with RM51 and sharing its resolver: nothing in a spec listing says which files a human wrote and which the enricher produced, so a registry gave publishers a `derived/` tree and found it is one `compile` refuses; their layout stays transport-only (flatten on upload, re-split on download). The byte-attestation half of S26 shipped as `manifest.derived` in 0.6.0; this is the half that did not. Not the one-line fallback it looks like: `spec_dir / "resolution.csv"` is resolved in **eight places across two packages**, and tolerating the layout on *input* without deciding the *write* side breaks on first use — `enrich` on a downloaded split module writes to the root, producing the both-copies-present collision by following the documented workflow. Three candidate repairs named and refused: search any subdirectory (blinds `_check_misspelled_tables`, re-opening S16's hole), make `derived/` canonical (two supported layouts, and `reverse` emitting a tree older compilers in the same major cannot read), extend it to authored tables (two legal homes for `variants.csv`, so the ignored copy is invisible). Shipped as `just_dna_format.layout` — tolerated, never canonical; write to the file you read; both present is an error naming both. Two things the item did not record: `_check_misspelled_tables` had to be extended into the subdirectory or a single fixed name blinds it just as a tree walk would, and `manifest.derived` carries the relative path so a re-splitting registry can use it. · *also in* CHANGELOG, CONSUMER_SUGGESTIONS_HISTORY § S26

- **[RM51](ROADMAP_HISTORY.md#rm51--licensingcsv-land-the-better-name-in-a-minor-so-the-major-only-has-to-remove)** — ✅ **shipped in 0.6.0**: accept `licensing.csv` as a second spelling of `sources.csv` in 0.6 (enricher writes it, compiler falls back to it), so the 1.0 rename only has to **remove** a spelling rather than add one. Minor-legal for a checked reason: the fact sidecars are **not** in `_INPUT_FILES`, so the filename enters no identity — `content_signature` is rows, `source_signature` is facts, `manifest.derived` is transport-only. What cannot come along is `sources.parquet` (inside `artifact.digest`, read by name) and the `manifest.sources` key — both removals, both major — so the 0.x tail reads `licensing.csv` → `sources.parquet` → `manifest.sources`, which is the cost. Both files present is RM49's collision (fact-hashed and human-overridable, so no merge and no newest-wins) — write to the file you read, error naming both; the two items shipped together for exactly that reason. The item estimated **five** enricher write sites and there were **nine**, so `record_source_terms`/`merge_sources_file` take the spec directory now and no pass names a spelling by hand. Old spelling deprecated in 0.6, removed at 1.0. · *also in* CHANGELOG, SCHEMAS, COMPILER, ENRICHER

## ✅ The 0.6 design round — decided, then built

*All decided on 2026-08-13 in [PROPOSAL_0_6.md](PROPOSAL_0_6.md) and **all built** in the batch that
followed. **Eleven have their own outcome entry** — RM4, RM5, RM24, RM25, RM27, RM43, RM45, RM46, RM47,
RM48, RM50 — and their numbers link to it in [ROADMAP_HISTORY § 0.6.0](ROADMAP_HISTORY.md#060--the-design-round-built),
which records what probing changed along the way. **The VCF 4.4 cluster deliberately has none**: RM53–RM65
came from [VCF_4_4_AUDIT.md](VCF_4_4_AUDIT.md), which stays their evidence document (spec quotations,
`file:line`, probe transcripts) and would only be restated by a history section, so those numbers link
into the proposal instead. Either way the proposal stays the authoritative record of the **reasoning**:
the problem, the facts probed, the decision, **the repairs that were rejected and why**, and the
consequences that follow without being chosen. Where an entry below and the built result disagree, the
history entry wins.*

*Landed on the `0.6` branch in eleven parallel lanes plus a charter amendment that went first and alone
(schema-change cost by layer: a parquet column is ~free, a derived CSV half, an authored schema full —
four of the decisions turn on it). Corpus effect of **that** batch, measured across the eleven reference
examples it had: `content_signature` moved on **two** modules (both re-authored on purpose),
`artifact.digest` on seven, `resolution_signature` was **gained** by the four table-only modules that
never had one, and the source signature moved nowhere. Suite 1535 → 2046. Left as that round's own
numbers rather than refreshed — a measurement of a batch is not a running total.*

*A second round followed on **2026-08-14**: dogfooding the batch produced five probe modules, a corpus
sweep and the findings ledger [DOGFOOD_0_6_FINDINGS.md](DOGFOOD_0_6_FINDINGS.md), then twelve parallel units fixed
what it classed `fix` (PRs #12–#23; the release view is the newest [CHANGELOG.md](CHANGELOG.md) entry).
**It changed no decision** — where it changed what an item shipped as, the entry below says so, and the
five findings whose repair is itself a design decision became RM68–RM72 in the 0.7 section. Its own
corpus effect, over the sixteen examples there are now: `content_signature`, `resolution_signature` and
the source signature are unchanged on every module, and `artifact.digest` moved on the three carrying
`literature.csv`, where `LiteratureRow` gained an optional `doi_checked`. Suite 2046 → 2221.*

- **[RM4](ROADMAP_HISTORY.md#rm4--native-clinvar-gene-panel-materialization)** — gene-panel materialization. **Off the compiler**: the surface is enricher draft-scaffolding, and the author's no-op over the drafted subset is the authorial act. Takes the `panel:` block with it (deprecated, removal at 1.0; the tautology check now keys on a ClinVar release `clinvar_draft` stamps into the licence row, both sides calling one shared label function) and re-keys that check on a mode ladder — `strict` audits every row into copied / authored / conflicting / no_record. Two findings came out of building it: a never-clobber merge turns a machine-stamped release label into a stale claim, so it is *withdrawn* rather than re-labelled; and "copied" had to become allele-exact, since a locus-wide match can be a sibling allele's call. · *also in* CHANGELOG, CLAUDE.md, ENRICHER, SCHEMAS, USE_CASES, ROADMAP_1_0, PROPOSAL_0_6
- **[RM5](ROADMAP_HISTORY.md#rm5--symbolic--structural-alleles)** — symbolic / structural alleles. **The five closed VCF first-level types and nothing beyond the standard** — no declaration mechanism, no named aliases. The length rides **inside the token** (`<DEL:1500>`, `<CNV:TR:30>`), decided on evidence: SVLEN is `Number=A`, so a scalar column cannot describe `alts=<DEL:5>,<DUP:9>` and three of the columns holding an allele have no row to hang a length on. A lengthless one is dropped with a warning that says *dropped*, refused under `strict`, and fatal in both modes on the composite tables. 5-HTTLPR is a plain indel; CPIC's IUPAC codes stay unexpressible, deliberately. **The 2026-08-14 round found the tier this was never told about**: `enrich` crashed outright on a symbolic allele, and offline the compiler called it an indel and prescribed a re-run that was the crash. Both fixed, with a permanent reason class rather than an `--offline` limitation, and the lengthless message now names the token it was given instead of always saying `<DEL>`. · *also in* CHANGELOG, CLAUDE.md, COMPILER, ENRICHER, SCHEMAS, USE_CASES, REFERENCE_EXAMPLES, DOGFOOD_0_6_FINDINGS §§ D1, D3, PROPOSAL_0_6
- **[RM24](ROADMAP_HISTORY.md#rm24--genedisease-validity-as-a-table)** — gene–disease validity. **Built as a derived sidecar**: `gene_validity.csv`, machine-written, half cost, its own signature and manifest block, and a new non-tainting `gene_validity` source layer. Probing moved the stated key twice — mode of inheritance joins it (59 ClinGen gene/disease pairs differ only there) and so does `submitter`, because GenCC is a nineteen-submitter aggregate whose *disagreement* is the data. **HPO ships no route**: its licence URL 404s and OBO Foundry records no SPDX id, so its terms cannot be established, and an inject-only tier does not assume them. · *also in* CHANGELOG, CLAUDE.md, COMPILER, ENRICHER, SCHEMAS, PROPOSAL_0_6
- **[RM25](ROADMAP_HISTORY.md#rm25--clinvar-assertion-tier-as-artifact-data)** — ClinVar assertion tier. **Built as a derived sidecar**, `clinical_assertions.csv`: the clinical call, the review wording, the star rating and ClinVar's own VariationID, one row per allele × record. Compute-and-discard, the fourth instance — `draft_gene_panel` was already filtering on the star rating and throwing it away. A one-star single submission and a practice guideline are no longer flattened to the same `clin_sig`. The cross-check's severity stays parked, deliberately. · *also in* CHANGELOG, COMPILER, ENRICHER, SCHEMAS, PROPOSAL_0_6
- **[RM27](ROADMAP_HISTORY.md#rm27--a-redistribution-compile-gate)** — redistribution. **Record only**, verdict in the manifest; no gate in these four packages, because gating belongs at *publish* and publish lives downstream. It ships with an explicit ask addressed to the registry in SCHEMAS.md rather than an implication, which is the whole difference from the status quo it was filed about: a recorded right nobody is told to enforce, versus a recorded right with a named enforcer. · *also in* CHANGELOG, CLAUDE.md, COMPILER, ENRICHER, SCHEMAS, PROPOSAL_0_6
- **[RM43](ROADMAP_HISTORY.md#rm43--resolution-reaches-the-snp-core-only-so-a-04-led-module-is-rsid-joinable-and-nothing-more)** — resolution reaches the SNP core only. **The positional parquets are filled** now, in `validate_spec` as well as `compile_module`, each model gaining stamped parquet-only `variant_key` and `authored_ident` plus `alts` on `PharmVariantRow`/`HaplotypeRow` as **data, not identity**. No `resolution.parquet`; reverse rebuilds the lookup table from the positional parquets, which P7 forces. `pgx_slco1b1_simvastatin` went from nine all-null rows to `12 / 21178615 / T / A,C`. One deviation kept: the stamped fields are `Field(exclude=True)`, or `content_signature` moves on all five positional modules — which leaves `VariantRow`'s own two inconsistent with them, grandfathered and filed as a 1.0-cleanup candidate. The fill is skipped off GRCh38 (RM15), which is what **RM69** is about. · *also in* CHANGELOG, CLAUDE.md, COMPILER, ENRICHER, SCHEMAS, CONSUMER_SUGGESTIONS_HISTORY § S9, ROADMAP_0_7, PROPOSAL_0_6
- **[RM45](ROADMAP_HISTORY.md#rm45--the-manifest-is-rich-about-resolution-and-silent-about-verification-so-unchecked-and-clean-are-one-state-to-a-downloader)** — the manifest cannot say what was verified. Shipped as `verification.json`, a derived attestation the enricher writes and the compiler stamps into `manifest.verification`, dropping it with a warning when stale: counts rather than booleans, **two** fields rather than one union-typed slot, two closed vocabularies, hash-bound with a ~0.4 s median proof-of-work per sidecar and a deterministic nonce, every field marked untrusted because a forged pass is worse than silence. A JSON document rather than a fifth fact CSV. Depended on RM43. **The 2026-08-14 round found the vocabulary half-wired** — twelve of the seventeen check members were emitted by nothing. Six were wired then (three from `literature`, one each from `pgx` and `vrs mint`, and rsid↔coordinate inside `enrich`, a check that turned out to be unreachable from `enrich()` at all); the remaining six are **RM72**. · *also in* CHANGELOG, CLAUDE.md, COMPILER, ENRICHER, SCHEMAS, CONSUMER_SUGGESTIONS_HISTORY § S8, DOGFOOD_0_6_FINDINGS § D4, PROPOSAL_0_6
- **[RM46](ROADMAP_HISTORY.md#rm46--a-literature-sources-terms-are-per-article-so-the-enricher-names-a-source-it-cannot-record)** — per-article literature terms. **Four licence columns on the derived literature row**, filled from the Europe PMC response the pass already makes and mapped at read time, so a mapping fix reaches rows already written. No `PUBMED_TERMS` constant: one "pubmed, fine" row would clear a module quoting a CC-BY-NC article, which is wrong in the dangerous direction. Quoting a restrictive article **warns, never gates** — arbitrating copyright is the same overreach as arbitrating a clinical dispute. · *also in* CHANGELOG, CLAUDE.md, ENRICHER, SCHEMAS, CONSUMER_SUGGESTIONS_HISTORY § S10, PROPOSAL_0_6
- **[RM47](ROADMAP_HISTORY.md#rm47--a-bin-boundary-is-the-most-interpretive-claim-in-the-format-and-the-only-one-with-nowhere-to-cite)** — a threshold has nowhere to cite. **A pointer on the bin row** — `MeasureBinRow.pmid`, one optional column on the binning base reaching all four kinds — **and the citation subject relaxed**, so a `studies.csv` row may name no variant at all. The bin cites, the citation table describes, which is what stops `StudyRow`'s provenance columns migrating onto binning rows one at a time. The same-release obligation was met rather than deferred: the compiler's cross-check and the enricher's literature pass both read the new site, reached through **public** compiler symbols rather than a private import. **Corrected on 2026-08-14**: the grounding check was also clearing a bin that carried a variant identity, on the reasoning that a study row could name it back — inside a branch that had already established the module records none — so a `heteroplasmy.csv` module stating four thresholds and citing nothing was silent while the same thresholds on `repeat_alleles.csv` were reported. · *also in* CHANGELOG, CLAUDE.md, COMPILER, ENRICHER, SCHEMAS, CONSUMER_SUGGESTIONS_HISTORY § S19, DOGFOOD_0_6_FINDINGS § D1, PROPOSAL_0_6
- **[RM48](ROADMAP_HISTORY.md#rm48--an-hg19-coordinate-has-no-supported-path-into-a-grch38-module-and-liftover-is-the-wrong-primitive)** — old-assembly coordinates. **rs-number recovery only, no liftover, no chain file** — the stated blocker was checked and is false, since Ensembl's GRCh37 REST service is permanent, unauthenticated and same-shaped. Reporting, never filling. The offline half (a position past its contig's end, a contig only one build names) went into the **compiler** as an error in both modes; the online build-guessing half stayed in the enricher and supersedes the ±1 shift reading only from its two strong evidence tiers. Its sensitivity is ~3 in 4 and now measured rather than asserted, so *"the compiler catches wrong-build coordinates"* is not a reading to take. · *also in* CHANGELOG, CLAUDE.md, COMPILER, ENRICHER, SCHEMAS, CONSUMER_SUGGESTIONS_HISTORY § S22, ROADMAP_0_7 (RM68), PROPOSAL_0_6
- **[RM50](ROADMAP_HISTORY.md#rm50--pmid-and-pmcid-are-one-id-apart-and-only-one-direction-of-the-conversion-exists)** — PubMed vs PubMed Central ids. Guard + the PMC id on the derived row + a reporting reverse lookup (`hint citation --pmcid`) that never fills `pmid`, because filling it would make the existence check compare NCBI with itself. The live hazard was the accepted spelling, not the refused one: `PMC 3110566` used to extract as PMID 3110566, a real id for an unrelated article, so a cell that compiled before may refuse now. The authored half is 1.0 with the requiredness demotion. · *also in* CHANGELOG, CLAUDE.md, ENRICHER, SCHEMAS, PROPOSAL_0_6
- **[RM53](PROPOSAL_0_6.md#rm53--a-bare-field-name-means-two-different-vcf-fields)** — a bare VCF field name means two different fields (`DP`, `AD`, `MQ`, `AF`, `CN`). Accept the qualified form; warn on a bare colliding name. **Both shipped reference examples were wrong and were re-authored**, which is why `mt_heteroplasmy` and `htt_repeat_expansion` are the only two modules whose `content_signature` moved across the whole batch. · *from* VCF_4_4_AUDIT § 1 · *also in* CHANGELOG, COMPILER, SCHEMAS
- **[RM54](PROPOSAL_0_6.md#rm54--a-pointer-at-a-multi-valued-field-cannot-say-which-value)** — a pointer at a multi-valued field cannot say which value. A **closed vocabulary of selection rules**, never an index. Shipped on the binning base's `source_field` **only** — `callable_element`/`quality_element` are **reserved, not built**: `callable_from` and `quality_from` can name a multi-valued field too and no module does, and an authored column on `variants.csv` is full cost under the 0.6 amendment. Additive whenever a real case arrives (P3), the names held in `RESERVED_NAMES_0_4` so they survive the one-way door, and everything reading `VCF_POINTER_COMPANIONS` is generic over it. The proposal reads as though the rule reached all three columns; this is the built shape. **The 2026-08-14 round carried the rule meanings onto the surface an author reads**: they reached only the whole-schema `reference` and not `describe <kind>`, and `largest`'s sentence explained the rule as *the longer of the sample's two alleles* — a diploid claim, corrected against FMR1 in a male. · *from* VCF_4_4_AUDIT § 2 · *also in* CHANGELOG, COMPILER, SCHEMAS, DOGFOOD_0_6_FINDINGS §§ D1, D5
- **[RM55](PROPOSAL_0_6.md#rm55--copy-number-and-repeat-count-are-not-whole-numbers-and-the-schema-says-they-are)** (0.6 half) — copy number and repeat count are not whole numbers. **0.6 warns loudly and changes nothing.** Fix in [PROPOSAL_0_6_PT2](PROPOSAL_0_6_PT2.md#rm55--a-fractional-copy-number-matches-no-bin-and-the-fix-is-not-the-column-the-entry-names) — taken back into 0.6 on 2026-08-16, so the fix and this warning now ship in the same line and the warning becomes conditional; removal in [ROADMAP_1_0](ROADMAP_1_0.md). `cyp2d6_structural` and `fmr1_cgg_repeat` are the corpus instances it fires on, each naming the field the spec does (`CN`, `RUC`). · *from* VCF_4_4_AUDIT §§ 3, 4 · *also in* COMPILER
- **[RM56](PROPOSAL_0_6.md#rm56--a-measurement-can-span-several-bins-and-there-is-no-state-for-that)** (0.6 half) — a measurement can span several bins. **0.6 withholds and says loudly that no policy exists**, once per table, and says that withholding is not the `unresolved` row. Policy vocabulary in [ROADMAP_0_7](ROADMAP_0_7.md). · *from* VCF_4_4_AUDIT § 4 · *also in* COMPILER, SCHEMAS
- **[RM57](PROPOSAL_0_6.md#rm57--a-quality-floor-inverts-on-exactly-the-records-it-exists-for)** — a quality floor inverts on reference records; callability evidence is a block, wanting interval containment and the block minimum. Docs + warning, and the warning names GQ and MIN_DP as the fix rather than leaving the author with the diagnosis. · *from* VCF_4_4_AUDIT § 5 · *also in* SCHEMAS
- **[RM58](PROPOSAL_0_6.md#rm58--alts---splits-identity)** — `alts="."` splits identity. A diagnosis, not a grammar; stop filing the MISSING marker beside symbolic alleles. **The only VCF finding reaching identity.** The 2026-08-14 round guarded the minter against it too — `.` raised the same unhandled `ga4gh.vrs` error a symbolic allele did. · *from* VCF_4_4_AUDIT § 6a · *also in* CHANGELOG, COMPILER, ENRICHER, SCHEMAS
- **[RM59](PROPOSAL_0_6.md#rm59--the-allele-that-could-not-be-observed)** — `*`, the allele that could not be observed. **Writable, and the contract states the withhold rule.** Decided beside RM5, deliberately not inside it — `*` names no variant, it makes an observability claim. The 2026-08-14 round found the sharper half: `*` **passes** `ga4gh.vrs`'s sequence pattern, so an unobservable allele reaching the minter would have been handed a content-addressed id for a state that is not a sequence. Guarded on the enricher side. · *from* VCF_4_4_AUDIT § 6b · *also in* CHANGELOG, COMPILER, ENRICHER, SCHEMAS
- **[RM60](PROPOSAL_0_6.md#rm60--chrom-rejects-chrm)** — `chrom` rejects `chrM` while a normalizer in the same package accepts it. Widening only, and it did not blind RM48: `chrM:16600` is still refused against MT's 16,569 bp. · *from* VCF_4_4_AUDIT § 7 · *also in* CHANGELOG
- **[RM61](PROPOSAL_0_6.md#rm61--the-pointer-grammar-rejects-vcf-legal-field-names)** — the pointer grammar rejects VCF-legal keys (`1000G`, dotted). Widening only. · *from* VCF_4_4_AUDIT § 8a · *also in* COMPILER, SCHEMAS
- **[RM62](PROPOSAL_0_6.md#rm62--32-bit-values-against-64-bit-bounds)** — 32-bit values against 64-bit inclusive bounds. Consumer contract, no schema change — **and the rule as shipped was one-sided.** *Narrow the authored bound to float32* rests on `float32(0.3)` landing above `0.3`; `float32(0.9)` lands **below** `0.9`, so narrowing an authored `0.9` upper bound drops a row whose measurement never went through float32. The rule that survives is the one SCHEMAS.md's own heading always gave — **compare *in* float32** — corrected on 2026-08-14 and put on `measure_max`'s printed description, since the word "narrow" appeared zero times in what an author actually reads. · *from* VCF_4_4_AUDIT § 8b · *also in* CHANGELOG, SCHEMAS, DOGFOOD_0_6_FINDINGS § D6
- **[RM63](PROPOSAL_0_6.md#rm63--a-pipe-separated-genotype-names-no-homolog)** — a pipe-separated genotype names no homolog without a phase set. Shipped as a correction to a code comment, and carried onto the printed `genotype` description on 2026-08-14 — the pipe form was not in the authoring contract at all, while the validator accepted it and `phased` round-tripped. **The 0.6 replacement wording overclaimed in its turn**: `C|C` loads, so *"read a pipe as heterozygous"* is false of a genotype the model accepts. The printed description carries only the true half ("phase recorded but unaddressable"); the code comment it was copied from is still wrong, filed as R2-14. · *from* VCF_4_4_AUDIT § 8c · *also in* CHANGELOG, SCHEMAS, DOGFOOD_0_6_FINDINGS §§ D5, Round 2
- **[RM64](PROPOSAL_0_6.md#rm64--the-vcf-id-column-is-a-list)** — the VCF id column is a semicolon list. Documentation. · *from* VCF_4_4_AUDIT § 8e · *also in* SCHEMAS
- **[RM65](PROPOSAL_0_6.md#rm65--repeat-and-copy-number-tables-are-positional-and-the-code-says-they-are-not)** (0.6 half) — repeat and copy-number tables *are* positional and the code says they are not. **0.6 corrects the claim**; the coordinates wait in [ROADMAP_0_7](ROADMAP_0_7.md). · *from* VCF_4_4_AUDIT § 10 · *also in* ROADMAP

## ✅ The 0.6 dogfooding fix round — Round 2's own findings, repaired

*Round 2 of [DOGFOOD_0_6_FINDINGS.md](DOGFOOD_0_6_FINDINGS.md) came from reading the code around each
0.6 repair rather than from building a module, and its thirteen actionable findings were grouped into
RM74–RM79 on 2026-08-14. **All six shipped**, on 2026-08-15 — which cleared the sprouts before the ground: RM76's narrow repair is what landed here, and the root it grows from, RM73, closed a day later.
Outcomes in
[ROADMAP_HISTORY § 0.6 dogfooding](ROADMAP_HISTORY.md#06-dogfooding--the-fix-rounds-own-findings-repaired).*

- **[RM74](ROADMAP_HISTORY.md#rm74--the-drafting-providers-read-their-sources-wrong-and-the-test-that-would-have-caught-one-does-not-run)** — ✅ **shipped**: ClinPGx's `gene` is `;`-multi-valued and both readers treated the cell as one symbol — 396 of 16,087 snapshot rows, and `--gene VKORC1` silently dropped the 3 rows of `rs17886199` (`PRSS53;VKORC1`). The filter matches per member now. What a plural cell *writes* was the real choice and two candidates are decisively refuted: **one row per gene is illegal**, because `gene` sits outside `PharmVariantRow`'s dedup key and the copies collide where `drugs`' copies do not; and **picking from the cell has no rule to pick by**, since the pharmacogene is first in `CYP3A5;ZSCAN25` and second in `PRSS53;VKORC1`. What shipped is the CPIC `gene.chr` move — write the member the request selects, and otherwise **withhold and name the genes the cell held**. Also: `skipped_unidentified` was counted before the `--gene` filter, inflating "records the source could not identify" by the rest of the database (R2-11); and a location fixture keyed `"location"` where `cpic.defining_variants` reads `"sequence_location"`, so the file's claim to cover a coordinate-carrying defining variant was hollow and green either way (R2-3), now fixed with the key *and* an assertion that the coordinate reaches `haplotypes.csv`. · *from* DOGFOOD_0_6_FINDINGS § Round 2 (R2-1, R2-11, R2-3) · *also in* CHANGELOG

- **[RM75](ROADMAP_HISTORY.md#rm75--a-complete-result-is-destroyed-by-an-incidental-failure-and-one-handler-cannot-see-its-own-case)** — ✅ **shipped**: three instances of work that has already succeeded being thrown away. `CpicClient._get` wrapped only *shape* failures, so an exhausted retry ladder left a raw `httpx.HTTPStatusError` that walked through both of `enrich_pgx`'s per-leg handlers and took PharmVar's answer with it (R2-13) — the retrying half is `_request` now and the translation sits **outside** it, since wrapping inside would make `retry_if_exception_type` a no-op, with a test that the ladder survived the split. **The ledger named only CPIC and PharmVar had the identical hole**; repairing one leg would make *"one source failing must not sink the pass"* true in one direction only. `knows_drug` is caught (R2-4), so an optional message-enrichment call can no longer discard a finished draft, and the `bool | None` tri-state that was designed and never delivered from the live client is finally reachable — with a **third** wording, because "the snapshot cannot answer" and "the request failed" have different remedies. And `spec_genome_build`'s deliberate raise no longer tracebacks out of `draft`/`draft-panel` (R2-2): `_DRAFT_PRECONDITION_ERRORS` names the shared cause, and the check moved above the snapshot provisioning it used to be paid for. · *from* DOGFOOD_0_6_FINDINGS § Round 2 (R2-13, R2-4, R2-2) · *also in* CHANGELOG

- **[RM76](ROADMAP_HISTORY.md#rm76--an-unfinished-authoring-state-passes-every-gate-including---strict)** — ✅ **the narrow repair shipped**: a `<<REPLACE>>` stub in the licence table compiled green under `--strict` and reached `manifest.sources` inside the block its own signature covers. `SourceRow` is not an `AuthoredModel` (deliberately, as a machine-produced reference fact) and S21 made it draftable (deliberately, as the one fact sidecar a human writes); both were right and nobody reconciled them. The guard lands on the model rather than through the base — `ModuleSpecConfig` is the precedent — so the other four sidecars stay out, since no template is generated for them. Two things worth carrying: a **vocabulary** column was catching the stub by accident, which is why the free-text half stayed open *and* why this item's first test came out green on the unfixed code; and `stub_template`'s printed guarantee (*"an unreplaced stub cannot compile"*) is now asserted over every `DRAFTABLE` kind, having held only where a model happened to inherit the right base. **The general question — what marks authoring as unfinished — is [RM73](ROADMAP_HISTORY.md#rm73-phase-boundary--authoring-is-a-process-and-it-now-has-an-end)**, which closed on 2026-08-16. · *from* DOGFOOD_0_6_FINDINGS § Round 2 (R2-8) · *also in* CHANGELOG

- **[RM77](ROADMAP_HISTORY.md#rm77--the-genotype-diagnosis-tells-the-author-about-the-wrong-thing)** — ✅ **shipped**: a genotype pasted from a VCF (`0/1`) fell through to the nucleotide-grammar wall and was never told those are allele *indices* into the record's REF/ALT list — the single most likely mistake in this column, since pasting `GT` is the obvious first guess. `0/1/1` was worse *because of* 0.6: the ploidy fix gave it a confident, correct explanation of the two-allele ceiling, which is about the wrong thing, so the diagnosis runs **ahead of the arity branch** and a test pins that a base-spelled triploid still gets the ceiling message. Changes no verdict — every matched cell was already refused — and reaches `PharmVariantRow` free, since the grammar has lived on `AuthoredModel` since 0.5. Second half: **RM63's own replacement wording was false**, since `VariantRow(genotype="C|C")` loads and `1|1` is an ordinary phased homozygote; the printed `describe` output had already dropped the zygosity word, so only the comment it was copied from was wrong. A correction is where this happens — the reviewer checks the claim being removed, not the one going in. · *from* DOGFOOD_0_6_FINDINGS § Round 2 (R2-9, R2-14) · *also in* CHANGELOG

- **[RM78](ROADMAP_HISTORY.md#rm78-the-two-fixes--the-unobservable-marker-and-a-guard-that-answered-its-own-question-wrong)** — ✅ **shipped, all three parts.** `*` no longer lands in the compiler's indel bucket (R2-6 — filed as having no instantiation, upgraded when `*` turned out to **pass** `LiteralSequenceExpression`'s `^[A-Z*\-]*$`; note *severity* could not have caught it, since the indel branch it fell into is also tier-blame, so only the reason can). `refget_supports_build` no longer answers `True` for the two inputs `refget_accession` raises on (R2-10 — the old reasoning imported a spec-layer default into the identity layer, and every other build gate in `vrs` already read it the other way). And the decision (R2-5): a **stored** VA against a symbolic allele is now the row's contradiction, an error in both modes, [**settled grammar-first**](ROADMAP_HISTORY.md#rm78-the-decision--a-stored-vrs-id-against-a-symbolic-allele-is-the-rows-contradiction) — `validate_vrs_allele_id` makes both `vrs_id` columns `ga4gh:VA.`-only (no instantiation: 844 corpus ids, all VA) so a present id can only name a *different* allele, and only then does the escalation follow. The asymmetry is pinned: an **absent** id on the same row stays a warning, because absence is a limit and a claim is a claim. · *from* DOGFOOD_0_6_FINDINGS § Round 2 (R2-6, R2-10, R2-5) · *also in* CHANGELOG

- **[RM79](ROADMAP_HISTORY.md#rm79--two-honest-counters-disagreed-so-the-compiler-stopped-carrying-the-dead-weight)** — ✅ **shipped**: `manifest.literature.missing_count` counted the whole table while the `citation_existence` record counted the module's current citations, so the two disagreed in a published manifest with nothing wrong in the module. **The item's own framing was the wrong question** — both blocks already publish their denominator, so the reconcilable-or-not choice was not the live one; what nobody had decided is why a row nothing joins to is in the artifact at all. The compiler discards it and `literature.csv` keeps it, so the counters agree **by construction** rather than by documentation. Four things pinned: the check sees every row and everything after it sees the kept ones (reporting needs the full list); an empty citation set discards nothing (a module citing nothing cannot tell a stale sidecar from unauthored citations); **both** citation sites count, and the stakes rose — blind to bin `pmid`s the compiler would now *discard* a threshold's evidence rather than warn about it; and the round trip narrows once and **converges**, which is what makes a derived-sidecar narrowing tolerable at all. No corpus movement, and no corpus coverage either — pinned by fixtures. · *from* DOGFOOD_0_6_FINDINGS § Round 2 (R2-16) · *also in* CHANGELOG

## ⏳ Deferred to 0.7 — [ROADMAP_0_7.md](ROADMAP_0_7.md)

- **[RM16](ROADMAP_0_7.md#rm16--authored-prs-weights-a-scoring-file-not-a-manifest)** — authored PRS weights. Not derivable, so a full-cost authored table, and no consumer combines authored weights into a score. · *also in* SCHEMAS, PROPOSAL_0_5
- **[RM23](ROADMAP_0_7.md#rm23--computational-predictor-scores-as-a-table)** — predictor scores (`predictions.csv`). Both blockers unmoved: per-transcript grain, and the acquisition measurement. Licensing is **not** the blocker. · *also in* CLAUDE.md
- **[RM28](ROADMAP_0_7.md#rm28--meta-conclusions-the-predicate-half)** — meta-conclusions, the predicate half only. Parked on a corpus. **The general injected-cofactor mechanism was dropped on 2026-08-13** as never-earned; each class gets a plain column on demand. · *also in* COMPILER, REFERENCE_EXAMPLES, PROPOSAL_0_5 § G3
- **[RM55](PROPOSAL_0_6_PT2.md#rm55--a-fractional-copy-number-matches-no-bin-and-the-fix-is-not-the-column-the-entry-names)** (the fix) — **taken into 0.6 PT2 on 2026-08-16, and the probe overturned the shape.** The bin bounds are *already* `float | None` (`binning.py:231`, `:238`) and `_INTEGER_KINDS` has **one** code reader (`binning.py:688`), so "a parallel float column beside the integer one" names a column that mostly does not exist. The unstated half of the defect is that the shared-endpoint rule **refuses the continuous tiling that would fix it** (`binning.py:666-677`). Decided, in two halves: an optional `measure_tiling` column, `{quantised, continuous}`, **absent = the kind's current default** so every published module keeps its meaning (a sixth measure kind is refused under P5 — kind is *what*, tiling is *how divided*); and `modifier_copy_number: float | None` beside `modifier_cn`, the one genuine `int`, read through an effective-value alias falling back to `float(modifier_cn)`, both-set an error, **`_KEY_FIELDS` keying on the effective value** so the key never holds two spellings (which is what answers the RM81 objection — the ambiguity never reaches the key). `modifier_cn` deprecated in 0.6 and removed at 1.0, so 1.0 inherits a **removal**, not a retype. **A fractional value switches the group to the continuous rules by itself, and says so** — the inference runs one way only, because fractional-ness is incompatible with quantised semantics while integer-ness implies nothing (`[0,1] [2,3]` may be a continuous measure whose author never saw fractional data). An explicit `quantised` beside a fractional value warns rather than being overridden. Nothing that validates today stops validating: `measure_min=2.5` already loads and now gets answered, which is a widening. · *also in* ROADMAP_0_7 (stale in its suggested fix, marked so), ROADMAP_1_0 (re-dated)
- **[RM56](ROADMAP_0_7.md#rm56-policy-half--the-rule-for-a-measurement-that-spans-bins)** (the policy) — withhold / worst bin / point estimate, as a closed vocabulary. Gated on real caller output; grain undefined on purpose.
- **[RM65](ROADMAP_0_7.md#rm65-implementation-half--repeat-and-copy-number-tables-are-positional)** (the coordinates) — gated on a real repeat-caller or CNV VCF, or a consumer report. Would take RM43's lane from three tables to five. The 2026-08-14 round supplied the *corpus* half of that evidence — `cyp2d6_structural` and `fmr1_cgg_repeat`, filed as evidence rather than as defects — and left the caller-output gate exactly where it was.
- **[RM66](ROADMAP_0_7.md#rm66--one-repeat-locus-several-motifs)** — one repeat locus, several motifs; the key cannot say which count the thresholds describe. Same prerequisite as RM65, and `fmr1_cgg_repeat`'s AGG interruptions against a `(gene, repeat_unit)` key are now the worked case.
- **[RM67](ROADMAP_0_7.md#rm67--polyploid-and-partially-phased-genotypes)** — polyploid / partially-phased genotypes. **Not work** — a documented divergence, numbered so it is findable and not re-probed. Unchanged by the 2026-08-14 round, which changed only the *message*: a polyploid call used to be refused with a bare restatement of the grammar, so a deliberate limit read as a syntax error; the refusal now says in-line that two alleles is a decision and that VCF 4.4 §7.2 permits more.

*The five 0.6 dogfooding findings the ledger classes **surface** rather than **fix** — the repair is
itself a design decision, so each entry carries the candidates and why each one fails. Found by the
probes of [DOGFOOD_0_6.md](DOGFOOD_0_6.md) on 2026-08-13 and filed on 2026-08-14 by the round
that fixed the rest; the ledger is [DOGFOOD_0_6_FINDINGS.md](DOGFOOD_0_6_FINDINGS.md), whose closing
table maps finding to unit to pull request.*

- **[RM73](ROADMAP_HISTORY.md#rm73-phase-boundary--authoring-is-a-process-and-it-now-has-an-end)** — ✅ **closed on 2026-08-16, both halves**, the root item.

  **The phase boundary.** Authoring had no end, so nothing could say a module was finished. RM45 had
  already built almost all of it for another purpose — `verification.json` binds the authored bytes and
  the compiler drops the block on any mismatch — so what shipped is one optional `Closure` block on that
  document, a `just-dna-compiler close` command, and a warning when a compile publishes none. No new
  file, no new binding, no new proof-of-work: the closure sits outside `pow_digest`'s payload, so
  closing re-mines nothing and every attestation written before it still verifies. Deliberate by design
  (`validate` stays read-only — a mark left by whatever ran says only that something ran), signable with
  the existing Ed25519 key, refusing an invalid spec but never a warning. Measured: all sixteen
  reference examples compile byte-identically before and after being closed, and all sixteen now ship a
  closure with a test that fails loudly when an authored edit outdates one. The enricher's merge had to
  learn to carry it — the never-clobber trap a third time, after `SourceRow.dataset` and `draft_digest`
  — and it drops rather than re-binds a closure whose bytes have moved, since only the author may make
  that claim again. **What is left is the promotion, not a half:** requiring a closure to compile is
  major-only (P8) and **blocked** — [ROADMAP_1_0 § RM73 (gate half)](ROADMAP_1_0.md#rm73-gate-half--the-closure-gate-refuses-on-step-3-of-the-round-trip),
  because reverse cannot re-emit the document, so under a gate `compile → reverse → compile` refuses on
  step 3. The probe that found it is the reusable half: warnings feed no digest and no signature, which
  is why the identical asymmetry has been free since RM45 and why the gate is what gives it a price.

  **The provenance half**, shipped the same day and the one the sprouts were actually asking for. A flat CSV row records nothing about how it came to be, so every check needing that had guessed: a drafting provider hashes the table it wrote, projected onto the column its own cross-check later reads, and stamps `SourceRow.draft_digest`; the check recomputes it and skips only on a conjunction of *this release* **and** *nothing moved*. Scoped to the checked **column**, not the row — a drafted module always has edited rows, since `genotype` is a stub the human must fill, so a row hash would never match. Closed two tautologies nobody had filed (CPIC/`allele_function`, ClinPGx/`pgx_evidence_level`, the latter already stamping the marker nothing read), made the skip per-**leg** in `enrich_pgx` so PharmVar still runs, and **deleted** RM4's `strict` per-row audit along with the mode ladder under it. The classification is derived, never typed. · *from* the RM72 read-through, 2026-08-14 · *also in* CHANGELOG, CLAUDE.md, COMPILER, ENRICHER, SCHEMAS, ROADMAP_1_0, the create-module skill
- **[RM80](ROADMAP_HISTORY.md#rm80--annotationsparquet-had-no-column-for-the-thing-that-distinguishes-its-rows)** — ✅ **shipped in 0.6.0**, retro-filed from a downstream consumer report: `annotations.parquet` is not unique on `variant_key` and never could be (poly-effect is real), and the authored column that decides *which call* an annotation applies to was in no column. `genotype` is now carried **and keyed on** — carrying it without keying would let two genotypes sharing a conclusion collapse onto a row naming one of them, turning a missing answer into a wrong one. Reverse learned to read which of three keyings an artifact carries. · *from* CONSUMER_SUGGESTIONS § S29 · *also in* CHANGELOG
- **[RM68](ROADMAP_0_7.md#rm68--a-drafting-provider-on-a-non-grch38-module-refuse-or-strip-to-the-rsid)** — a drafting provider on a non-GRCh38 module: refuse, or strip to the rsID. The **warning** shipped in 0.6 (`enrich.source_build_mismatch`); the behaviour is open. Refusal makes a GRCh37 module undraftable; stripping drops the 36 CPIC defining variants that have a position and no rsID. Probably dissolved by RM15. · *from* DOGFOOD_0_6_FINDINGS § D2 (F1)
- **[RM69](ROADMAP_0_7.md#rm69--resolution_signature-is-not-a-round-trip-invariant-when-the-positional-fill-is-skipped)** — `resolution_signature` moves across the round trip on a non-GRCh38 module carrying a positional table. **Not a Principle 7 breach** — `resolution.csv` is a derived sidecar, and P7's own next sentence (*"the artifact is missing a field, not the spec"*) is the diagnosis. Blocked on RM15. · *from* DOGFOOD_0_6_FINDINGS § D2 (F8), measured in § D6
- **[RM70](ROADMAP_0_7.md#rm70--requires_callable-is-variantrow-only-so-no-pgx-table-can-state-cpics-core-assumption)** — `requires_callable` is `VariantRow`-only, so a star-allele module cannot record the assumption CPIC states in prose. Additive and minor-legal; the open question is which of the three PGx tables owns it — `haplotypes.csv` and `pharm_variants.csv` name a position, `diplotypes.csv` does not. **Not** gated on RM65/RM66's caller VCF. · *from* DOGFOOD_0_6_FINDINGS § D2 (F11)
- **[RM71](ROADMAP_0_7.md#rm71--the-alleles-a-drafted-genotype-stub-must-be-written-from-are-in-no-file)** — a drafted `genotype` stub must be written from alleles the file does not carry, emitted once to stdout and not re-requestable. Every legal home is also the wrong home; the real question is where an author does the work. · *from* DOGFOOD_0_6_FINDINGS § D4 (D4-3)
- **[RM72](ROADMAP_0_7.md#rm72--six-verification-members-still-emitted-by-nothing-and-the-writes-nothing-contract)** — the remainder of D4-1 after six members were wired: **four** blocked on the printed *"Writes nothing"* contract of `check-identifiers`/`check-acmg`, **two** deliberately reserved (`gene_disease_validity`, `dosage_sensitivity`), one open question about read-only surfaces, and one about the merge rule — `merge_records` is *newest wins, per check*, so an offline re-run downgraded a real `subjects=2 findings=1` verdict to `skipped=offline`. **Taken into [0.6 PT2](PROPOSAL_0_6_PT2.md#rm72--the-verification-vocabulary-is-half-wired-and-the-contract-that-blocks-it-is-a-blanket-over-a-narrow-reason) on 2026-08-16**: the four are wired **unconditionally** (a `--attest` flag makes the record optional, and an optional record is ambiguous between *not run* and *ran without the flag*); `merge_records` stops letting a `skipped` replace a `ran`, on the function's own docstring argument one step further out; and the reword is **three sites, not the printed-contract surface the entry feared** — 37 sites carry *a* form of the promise but the other 34 are a different promise (`hint`/`lookup` write no authored *cell*, `validate` stamps no closure), and the `create-module` skill annotates **neither** command being changed (`SKILL.md:592-593`). The two reserved members do not move; each gains a line saying it is reserved. · *from* DOGFOOD_0_6_FINDINGS § D4 (D4-1)

*The five filed on 2026-08-16 out of [MODULE_LIFECYCLE.md](MODULE_LIFECYCLE.md), which mapped the second
pass and found that nothing had ever described it. They were that document's closing "open questions"
section until they were numbered — the shape this repo has twice found to be a backlog nobody reads, so
the section is gone and these are the whole of it. Of its seven questions, one dissolved into a
**documentation fix** (the trust rule's reader is a catalog, S34 — corrected in SCHEMAS.md and in
`manifest.py`'s own comment) and one was **decided**: a review stamp is an attestation of zero changes,
so un-closing the module is correct. That decision is what produced **RM86** — settling the format side
sent someone to read what the catalog does with the result, which turned out to be the sharper half.*

- **[RM82](ROADMAP_0_7.md#rm82--the-attestation-binds-raw-bytes-so-an-editors-line-endings-un-close-a-module)** — the attestation binds **raw bytes**, so normalizing line endings un-closes a module without a cell changing. **Decided 2026-08-16: normalize `\r\n` → `\n` before hashing and stop there** — no loader, no parse, which is what separates it from the content-aware binding RM45 refused. Costs a one-time re-attest of every `module_hash` in existence (soft: a stale attestation warns and is dropped); `manifest.inputs[]` deliberately does **not** follow. Sighted once before from the round-trip side in DOGFOOD_0_6's lane plan. **Taken into [0.6 PT2](PROPOSAL_0_6_PT2.md#rm82--the-attestation-binds-raw-bytes-and-size-is-inside-the-binding) on 2026-08-16, with the fact that decides the implementation:** `module_binding` *is* `artifact_digest` and `size=stat().st_size` is **inside** the hashed listing (`integrity.py:80-83`, `:106-112`), so normalizing the digest input while reporting the on-disk size still moves the binding on exactly the CRLF files — the naive fix is a no-op that looks like one. A separate normalized entry builder, reporting the normalized length, used by the binding and nothing else; not a `normalize=` flag on `file_entries`. · *from* MODULE_LIFECYCLE § 6.2 · *also in* MODULE_LIFECYCLE
- **[RM83](ROADMAP_0_7.md#rm83--a-derived-sidecar-can-only-be-refreshed-by-deleting-it-which-discards-the-overrides-it-exists-to-hold)** — merge-not-clobber means the only refresh is `rm`, which takes the curator's rows with it (three real `source=manual` rows in `cyp2c9_warfarin_grch37`); and the same rule means a source that silently **revised** a recorded row moves no signature, so §5.1's canary cannot fire without that same delete. One missing operation, both halves — a `--refresh` that re-asks and reports rather than merging. The charter's 0.6 amendment already names the class and RM45 discharged it for exactly one table. **Blocking sub-question: nothing records that a row was overridden**, so "keep the overrides" is not implementable today. · *from* MODULE_LIFECYCLE §§ 5.1, 6.3 · *also in* MODULE_LIFECYCLE, CONSTITUTION (the amendment)
- **[RM84](ROADMAP_0_7.md#rm84--a-module-has-no-version-identity-on-the-discovery-path-and-the-publisher-is-the-half-we-own)** — the HuggingFace discovery path carries no version, no digest and no manifest fetch, so the identity used to detect "the module changed" is a property of the **reader** (the consumer app's own package version), not of the module. Half of it is ours: `upload.upload_module` writes the flat `data/<name>/` layout. **Joint, and the consumer's half is already agreed in writing** — S34 §4 says they will follow a version segment if the publisher grows one. Had no `Sn` and no `RMn` before this entry. **Our half taken into [0.6 PT2](PROPOSAL_0_6_PT2.md#rm84--the-publisher-writes-a-path-that-cannot-express-a-version) on 2026-08-16**: it is one line (`upload.py:139`, `path_in_repo=f"data/{name}"`), and the decision is to **write both** — `data/<name>/v<version>/` beside the flat path, which keeps meaning *latest*, so neither side waits for the other and nothing already published has to move. `v<version>` verbatim rather than a bare `vN`, since a bare major segment collides two patch releases at one path. The one open thing is theirs: whether their fsspec scan matches `v0.6.0`. **✅ Our half shipped in the 0.6 PT2 batch (lane D):** `plan_upload` reads `identity.version` out of `manifest.json` and plans both paths (the versioned one is a **subdirectory of** the flat path, not a sibling), `upload_module` writes both, and a version that is absent/unreadable/unstated/not SemVer falls back to the flat path alone and **says which of the four** (`UploadPlan.version_unknown_reason`) rather than constructing `vNone`; `--dry-run` prints both. The one field is read from the JSON and gated with `is_valid_version` rather than validated through `read_manifest`, so an unrelated manifest defect cannot withhold a version that is legible. Two `upload_folder` calls, so **two HuggingFace commits, not one** — recorded rather than papered over, flat first because it is the path anything reads today. It refuses nothing new: `manifest.json` is in the allow-patterns and not in the required set, so a directory without one is still publishable. Two consequences documented rather than fixed: re-publishing without bumping `version:` overwrites the versioned copy (a policy question, not RM84), and the nesting is the second half of the ask. The row stays here because the **consumer's** half is still open, and with it the segment spelling, asked of them in ENRICHER § the publisher surface. · *from* MODULE_LIFECYCLE § 6.8 · *also in* CONSUMER_SUGGESTIONS § S34, ENRICHER, PROPOSAL_0_6_PT2
- **[RM85](ROADMAP_0_7.md#rm85--the-origin-of-a-module-predicts-the-shape-of-its-second-pass-and-nothing-records-it)** — a source-drafted module needs a refresh pass when the source moves and a paper-grounded one needs an evidence pass when the literature does; `SourceRow.dataset` records the release and **nothing acts on it**, so nothing tells an author their source has moved on. The tempting repair — a column stating the origin — is refused by RM71's argument one table over (it restates `dataset` and rots where `dataset` is maintained), so the live candidates read rather than write. Had no `Sn` and no `RMn` before this entry. · *from* MODULE_LIFECYCLE §§ 0, 7 · *also in* ROADMAP_0_7 § RM71
- **[RM86](ROADMAP_0_7.md#rm86--a-review-pass-is-legal-at-the-gate-refused-by-the-pre-flight-and-invisible-once-published)** — the downstream half of the review-pass decision, read out of the registry tree rather than assumed. The publish **succeeds** and the same-module carve-out is real code (so §6.6's long-standing claim is confirmed, not merely believed); but the **pre-flight answers `would_publish: false`** for that same publish, having no carve-out and never receiving the namespace — so an automated publisher branching on the documented field refuses a legal publish. `verification.json` is uploaded, stored, and read by nothing: outside `RECOGNIZED_SPEC_FILES`, dropped by every server-side rebuild, served by no endpoint (`provenance.json` one file over is recognised, so it is an omission not a policy). `authorship` reaches the manifest but no projected field. **Filed as an ask, RM27's shape** — delivered into the registry's own inbox on 2026-08-16 as **their S10, S11 and S12**, carrying the caveat that 0.6 is uncut so the half needing a reader of ours is explicitly *not* to be built against yet. The one question that is ours is whether a review should be a version at all, given that registry's own `reviews` table costs none, and it waits on their S12 reply. · *from* MODULE_LIFECYCLE § 6.6 · *also in* MODULE_LIFECYCLE §§ 6.6, stages 7–8, and `../just-dna-marketplace` CONSUMER_SUGGESTIONS §§ S10–S12
- **[RM87](ROADMAP_0_7.md#rm87--an-expanded-row-is-indistinguishable-from-an-authored-one-in-the-artifact)** — a one-to-many expansion pairs K authored genotypes with N loci, and only the member whose alleles can carry a genotype can match it; the others are well-formed rows asserting a pair the module never wrote, and **nothing in the artifact marks them** (`locus_index` is `resolution.csv`-only). Cost a consumer 3,762 false pathogenic findings, caught before rendering. 0.6 shipped the artifact-level half (`expanded_keys`/`expanded_rows` + the read-side contract); the row-level half is open. **Filed to correct a premise, not to defer one:** S33 ruled the parquet column out as a 1.0 conversation because it moves `artifact.digest`, and P3 plus the 0.6 cost amendment say the opposite — a stamped compiler-managed column is minor-legal and the cheapest class of change there is. What is genuinely open is *which* column: `locus_index` alone is `0` on both a non-expanded row and an expansion's first member, so it needs `locus_count` beside it, and a `bool` forecloses the ordinal under P5. **Decided in [0.6 PT2](PROPOSAL_0_6_PT2.md#rm87--an-expanded-row-is-indistinguishable-from-an-authored-one-and-the-weights-builder-reads-only-the-row) on 2026-08-16:** both columns, declared through `stamped_identity_field` (`exclude=True`, so no `content_signature` moves — a plain field would repeat `VariantRow`'s grandfathered defect), stamped at the expansion loop `resolution.py:178-188`, which already holds the ordinal and the total. `locus_count` defaults to **1**, not 0, or the predicate is not a predicate. `_build_weights` reads **only** from the row (`compiler.py:5027-5089`), which is why the value needs a field at all; reverse prefers the stored column over its encounter-order recompute (`compiler.py:6046-6062`) and keeps the recompute for pre-column artifacts. Owes the round-trip test nobody wrote — no test asserts a multi-member `0..N-1` sequence surviving `compile → reverse`. · *from* CONSUMER_SUGGESTIONS § S33 · *also in* SCHEMAS (the consumer join contract), COMPILER § Resolution

## 🔒 Deferred to 1.0 — [ROADMAP_1_0.md](ROADMAP_1_0.md)

- **[RM15](ROADMAP_1_0.md#rm15--build-agnostic-identity--multi-build-support)** — build-agnostic identity & multi-build support. A major because it changes the *semantics* of `variant_key` and of every coordinate — not for digest reasons. The build-naming half shipped as RM19. **Not RM48**, which is one-way and authoring-time. · *also in* USE_CASES, SCHEMAS, COMPILER, PROPOSAL_0_5
- **[RM52](ROADMAP_1_0.md#rm52--10-ships-an-upgrade-procedure-or-10-does-not-ship)** — the upgrade procedure. **Release-blocking by charter.** Decided 2026-08-13: the 0.6 batch owes **no** per-item ledger rows — that is premature decision-making, and the obligation belongs at 1.0.
- **[RM55](ROADMAP_1_0.md#rm55-removal-half--the-integer-copy-number-and-repeat-count-columns)** (the removal) — the integer columns and the integer tiling semantics. The only genuinely major part of the three-release route.
- **[RM81](ROADMAP_1_0.md#rm81--one-artifact-spells-a-genotype-two-ways)** — one artifact spells a genotype two ways: `weights.parquet` splits it into `List(Utf8)`, the 0.4-family tables keep the authored string, and a consumer joining both to one VCF meets two representations of one concept. A **retype** of a published column, so major-only; the parallel-column workaround is refused as two spellings in one table (the `vrs_id` desync shape). The *reader* half already shipped in 0.6 as `alleles.split_genotype`, the one public leaf every tier calls. Open question for the major: unify up (split everywhere) or down (verbatim everywhere). · *from* CONSUMER_SUGGESTIONS_HISTORY § S30 · *also in* SCHEMAS, CHANGELOG
- Plus the unnumbered items in [ROADMAP § The 1.0 cleanup](ROADMAP.md#the-10-cleanup-candidate-tracker), which stays where it is.

## ✖ Closed as an item

- **RM10** — declarative inheritance expectation. **Folded into RM28 on 2026-08-13**: it is an injected cofactor (family structure arrives at query time, exactly like ancestry), so designing it separately would fix a shape for one class before the axis exists. Reasoning lives in [ROADMAP_0_7 § RM28](ROADMAP_0_7.md#rm28--meta-conclusions-the-predicate-half). · *also in* USE_CASES, PROPOSAL_0_5

## — Not format scope

- **[RM7](ROADMAP.md#rm7--evaluation-output--report-card-schema)** — evaluation / report-card schema. Per-sample results are a *measurement*, so this is a **consumer** contract (`just-dna-lite`). Listed only so it is not mistaken for format scope. · *also in* USE_CASES, COMPILER, PROPOSAL_0_5

## 🔒 The major bucket — unnumbered on purpose, and that is why it hides

Everything needing a **major** lives in one table in
[ROADMAP § The 1.0 cleanup](ROADMAP.md#the-10-cleanup-candidate-tracker). These have **no `RMn`
number**, which is why they are easy to lose. They are not deferred features — each breaks a rule
Principle 3/8 protects, so a minor cannot carry it.

**Read this list under the amended cadence** (CONSTITUTION § 0.6 amendment, 2026-08-12): *deprecate in a
minor, remove at the next major*. An item whose replacement already exists takes its warn-only
deprecation in a 0.x release and is **gone at 1.0**, not deprecated at 1.0 and lingering to 2.0 — the
entries below still phrased the old way are stale, not decided. What genuinely keeps the old shape is
whatever Principle 8 makes mandatory (`state`, the `pathogenic`/`benign` booleans), because an author
cannot comply with a warning about a field they must still set. Every item here also owes an upgrade
line under **RM52**, which is release-blocking for 1.0.

- `VariantRow.state` — deprecate at 1.0, remove at 2.0; a derived alias of `direction` since 0.3
- `state` values `alt` / `ref` — drop from the read-vocabulary; genotype-relative descriptors
- `VariantRow.pathogenic` / `benign` — deprecate → remove; lossy aliases of `clin_sig`
- `StudyRow.p_value: str` — retype (the numeric companion `p_value_num` shipped in 0.5)
- `weights.parquet` `end` — **split by the charter amendment: wiring it is 0.6 work, only the removal is major.** Either way it needs an *end*-coordinate convention (interbase-half-open vs inclusive), the same choice RM15 must make. The authored `start` is settled: 1-based VCF POS, pinned by a test
- `weights.parquet` `likely_pathogenic` / `likely_benign` — remove; dead output, wiring rejected in 0.5
- `VariantRow.weight` vs `effect_size` — review whether `weight` is subsumed
- Deprecated flag / vocab aliases — collapse to the canonical vocab
- `ModuleManifest.authors` + free-form `curator` — fold into RM14's structured record
- `StudyRow.pmid` required — **doi-first**: require ≥1 of `{doi, pmid}`; a requiredness demotion. Pairs with RM50, which carries the PMCID axis and the `LiteratureRow` key
- `sources.csv` — **rename** (recommendation: `licensing.csv`). The *input* half is RM51 and shipped in 0.6.0; what waits for the major is `sources.parquet` (inside `artifact.digest`) and the `manifest.sources` key — both removals. The old CSV spelling is deprecated in 0.6 beside the alias and **removed at 1.0**, on the amended cadence this item prompted. It is a licensing/attribution ledger whose name collides with the `source` *column* meaning "which link answered" (the overload RM33 had to split) and with the ordinary sense in which `studies.csv`/`literature.csv` are sources too
- `fetched_at` — **rename** on all seven sidecar models (`updated_at` or `recorded_at`; the choice encodes a semantic decision about whether a provenance-column rewrite restamps). The name says *fetch*; measured, a re-run never restamps, so the value dates when the row's **facts** were set. Outside every fact set, so no signature moves and only `artifact.digest` does — which is why the disposition is to **bundle it with the `sources.parquet` rename above**, not to schedule it alone
- Compiler `ensembl_cache` shim — remove the deprecated parameter outright
- ~~Coordinate-first identity~~ — **resolved in 0.5** by VRS; kept struck through for traceability

## The other trackers

| Tracker | Where | What it holds |
|--------------|----------------------|----------------------------------------------------------------|
| Reserved namespace | [ROADMAP § Reserved namespace](ROADMAP.md#reserved-namespace) | Names withheld because a release will plausibly claim them (Principle 5). Three: `reference_db`, plus `callable_element` / `quality_element`, the two element-rule columns RM54 decided against building. |
| Freeform idea-book | [ROADMAP § Freeform suggestions](ROADMAP.md#freeform-suggestions--the-05-idea-book) | Unshaped ideas — the 0.5 book, plus **Parked in 0.5** (recorded so they are not re-proposed as new) and a [0.6 book](ROADMAP.md#freeform-suggestions--the-06-idea-book) below it. |
| Not format scope | [ROADMAP § Annotating core](ROADMAP.md#annotating-core-not-format-scope-the-05-source-assessment) | Half of every source assessed: anything that *calls or interprets* is a consumer's job. |
| Design threads | [PROPOSAL_0_6_PT2](PROPOSAL_0_6_PT2.md), [PROPOSAL_0_6](PROPOSAL_0_6.md), [PROPOSAL_0_5_1](PROPOSAL_0_5_1.md), [PROPOSAL_0_5](PROPOSAL_0_5.md), [PROPOSAL_0_4_1](PROPOSAL_0_4_1.md) | Where an item's shape was argued before it became an `RMn`, one file per release line — **two for 0.6**, because a second round of items landed behind the first and PT2 re-asked which release they belonged to. |
| Dogfooding ledgers | [DOGFOOD_0_6.md](DOGFOOD_0_6.md) (the plan) + [DOGFOOD_0_6_FINDINGS.md](DOGFOOD_0_6_FINDINGS.md) (what it found) | Findings from using the shipped CLIs on real modules. Only the `surface` half becomes an `RMn` — RM68–RM72 — so the `fix` half is findable **here and nowhere in this index**, by design. |
| What shipped | [CHANGELOG.md](CHANGELOG.md) | Newest first. The statuses above summarize it. |

## Where an item comes from

An `RMn` is stage 5 of the design cycle, not its start: a field report
([CONSUMER_SUGGESTIONS](CONSUMER_SUGGESTIONS.md)) → run it against the bricks
([USE_CASES](USE_CASES.md)) → shape it (the release's proposal file) → then either **shipped**
(recorded in [COMPILER.md](COMPILER.md)'s coverage table) or **parked as an `RMn`**. An item with no
trail through those was found by dogfooding or by an audit: RM31–RM35 came from the 2026-08-03 round,
RM53–RM67 from the VCF 4.4 audit, and RM68–RM72 from dogfooding the 0.6 batch on 2026-08-13.
